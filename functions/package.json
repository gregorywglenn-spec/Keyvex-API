{
  "name": "keyvex-functions",
  "description": "Firebase Cloud Functions wrappers for the KeyVex scrapers. Deployed via firebase deploy --only functions.",
  "type": "module",
  "main": "lib/index.js",
  "private": true,
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node22 --format=esm --outfile=lib/index.js --external:firebase-admin --external:firebase-functions --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\""
  },
  "dependencies": {
    "firebase-admin": "^13.9.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.2"
  }
}
